# IT-scoped configuration to ensure IntegrationTest app resolves defaultServiceName consistently in CI
# This makes the audit event's callingService field match the expected test JSON across all environments

defaultServiceName = "Entity Validation Service"
